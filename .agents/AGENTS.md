# Khelardesh Project Rules

## Git Push
After every code change, always run `git add -A && git commit -m "<message>" && git push` automatically — without waiting for the user to ask. Choose a meaningful commit message describing what changed.
